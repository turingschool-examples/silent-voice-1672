<h1> Studios </h1>

<% @studios.each do |studio| %>
  <div id=<%="studio-#{studio.id}" %>>
    <h3><%= studio.name %></h3>
    location: <%= studio.location %><br>
    Movies:
    <% studio.movies.each do |movie| %>
      <%= movie.title %>,
    <% end %>
  </div>
<% end %>